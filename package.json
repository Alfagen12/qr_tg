{
  "name": "telegram-qr-scanner",
  "version": "1.0.0",
  "description": "Web app for Telegram that recognizes QR codes using BarcodeDetector API",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server . -p 3000 -c-1",
    "dev": "npx live-server --port=3000 --no-browser",
    "build": "echo 'No build process needed for static files'",
    "deploy": "echo 'Deploy to your preferred hosting service'"
  },
  "keywords": [
    "telegram",
    "qr-code",
    "barcode-detector",
    "web-app",
    "camera",
    "scanner"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
